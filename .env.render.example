# Strapi on Render - Example Environment Variables

# Runtime
NODE_ENV=production
HOST=0.0.0.0
# Render asigna PORT automáticamente, este valor sirve como fallback local
PORT=1337
# URL pública de tu servicio en Render (o dominio personalizado)
URL=https://tu-servicio.onrender.com

# Database (Render PostgreSQL)
DATABASE_CLIENT=postgres
# Pega aquí el valor exacto de Render -> Connections -> External Database URL
# Incluye ?ssl=true si Render lo provee
DATABASE_URL=postgres://usuario:password@host:5432/mi_db?ssl=true
# Fuerza SSL en la conexión
DATABASE_SSL=true
# En Render normalmente no cargamos CA, por lo tanto deshabilitamos la verificación estricta
DATABASE_SSL_REJECT_UNAUTHORIZED=false
# Pool y timeouts (opcional)
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10
DATABASE_CONNECTION_TIMEOUT=60000
# Schema por defecto
DATABASE_SCHEMA=public

# Secrets (usa valores únicos para producción; no subas este archivo a git)
APP_KEYS=clave1,clave2,clave3,clave4
API_TOKEN_SALT=xxxxxxxxxxxxxxxx
ADMIN_JWT_SECRET=xxxxxxxxxxxxxxxx
TRANSFER_TOKEN_SALT=xxxxxxxxxxxxxxxx
JWT_SECRET=xxxxxxxxxxxxxxxx
ENCRYPTION_KEY=xxxxxxxxxxxxxxxx

# CORS (lista de orígenes permitidos); ajusta con el dominio de tu frontend
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://tu-frontend.com

# Telemetría opcional
STRAPI_TELEMETRY_DISABLED=true
